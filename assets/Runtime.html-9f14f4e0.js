import{_ as c}from"./plugin-vue_export-helper-c27b6911.js";import{r as a,o as t,c as s,b as e,d as i,a as o,f as n}from"./app-8999fdd0.js";const r={},l=n('<h1 id="runtime" tabindex="-1"><a class="header-anchor" href="#runtime" aria-hidden="true">#</a> Runtime</h1><h2 id="引子" tabindex="-1"><a class="header-anchor" href="#引子" aria-hidden="true">#</a> 引子</h2><p>学到OC，看到的文章中隐约中提到了OC的所谓“函数调用”其实是向持有类/实例发送消息从而达到调用效果，进一步了解OC的内存管理机制后，发现不同于C++，OC的内存管理有ARC、自动释放池这些“魔法”机制，而这些机制的又建立在这个神秘的Runtime机制上。</p><p>笔者的这篇文章大部分翻译自苹果，部分参考杨老师的文章，【】和<code>代码块</code>中是我添加的想法。</p><p><s>顺便改良了一下苹果的阴间排版。</s></p><p>参考文章：</p>',6),u={href:"https://yulingtianxia.com/blog/2014/11/05/objective-c-runtime/",target:"_blank",rel:"noopener noreferrer"},m=e("p",null,"杨老师基本上翻译了苹果的文章，加上的自己的理解，有些部分没有翻译，看的我满头大汗",-1),h=e("p",null,[e("s",null,"但是直接看感觉还是一知半解，而且全抄了我写什么。")],-1),v={href:"https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/ObjCRuntimeGuide/Introduction/Introduction.html#//apple_ref/doc/uid/TP40008048-CH1-SW1",target:"_blank",rel:"noopener noreferrer"},b=n(`<p>苹果这篇来自2009年的文章看的头疼，建议把侧栏关了然后放大看。Google翻译的稀烂，还得是看原文自己来。</p><p><s>发现chatGPT翻译的挺好的,这就是时代的进步吗</s></p><h3 id="为什么要引入runtime机制" tabindex="-1"><a class="header-anchor" href="#为什么要引入runtime机制" aria-hidden="true">#</a> 为什么要引入Runtime机制</h3><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>The Objective-C language defers as many decisions as it can from compile time and link time to 
runtime. Whenever possible, it does things dynamically. This means that the language requires 
not just a compiler, but also a runtime system to execute the compiled code. The runtime system 
acts as a kind of operating system for the Objective-C language; it’s what makes the language 
work。 —— 摘自苹果官方文档
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Objective-C 语言将尽可能多的决策从编译时和链接时<strong>推迟到运行时</strong>。只要有可能，它就会<strong>动态</strong>地执行操作。这意味着该语言不仅需要编译器，还需要运行时系统来执行编译后的代码。Runtime机制某种程度上充当了该语言的操作系统，让这门语言能够运作起来。</p><h2 id="与runtime交互" tabindex="-1"><a class="header-anchor" href="#与runtime交互" aria-hidden="true">#</a> 与Runtime交互</h2><p>与Runtime系统的交互可以分为三个级别</p><ul><li>通过源代码</li><li>通过类方法（来自Foundation 框架的NSObject类）</li><li>对runtime函数直接调用</li></ul><p>现在对这三种方法进行简单的介绍</p><h3 id="源代码交互" tabindex="-1"><a class="header-anchor" href="#源代码交互" aria-hidden="true">#</a> 源代码交互</h3><p><code>“在大部分时候,Runtime系统都自动地运行在幕后，你只需要编写和编译OC”</code></p><p>OC的编译器会为OC类和方法创造【符合动态特性的】数据结构以及方法</p><ul><li>数据结构会捕获类以及类的Category（分类）的定义、协议（Protocols）的声明以及方法选择器、实例变量模板以及其他来自源代码的信息</li><li>最重要的runtime函数就是<strong>发送消息</strong>的函数<code>Messaging</code>，这将在后面的文章提到它的源代码</li></ul><h3 id="nsobject方法交互" tabindex="-1"><a class="header-anchor" href="#nsobject方法交互" aria-hidden="true">#</a> NSObject方法交互</h3><p>大部分类是<code>NSObject</code>的子类,同时也继承了它的方法</p><ul><li>【最特殊的例外是 <code>NSProxy</code>，它是个抽象超类，它实现了一些消息转发有关的方法，可以通过继承它来实现一个其他类的替身类或是虚拟出一个不存在的类】</li></ul><p><code>NSObject</code>的方法因此为每个子类的实例和类对象建立了固有行为，当然也有些例外，<code>NSObject</code>仅仅定义了模版【一些事情应该怎么去做】而不提供实现代码。【<code>也就是所谓的抽象接口</code>】</p><ul><li>比如说<code>NSObject</code>定义了一个<code>description</code>实例方法【返回一个string描述这个类内容物】 <ul><li><code>重载它并为你定义的类提供描述内容</code></li><li>这主要用于GDB在debug时【<code>print-object</code> 指令】打印这个从描述方法返回的字符串 <ul><li><code>NSObject</code>的这个方法的实现不知道类包含了什么，所以它返回了一个字符串，包含了类名和地址。\`NSObject的子类可以实现这个方法以返回更多详细信息 <ul><li><code>Foundation</code>类NSArray返回其包含的对象的描述列表</li></ul></li></ul></li></ul></li><li><code>NSObject</code>的一些方法仅仅是用于查询Runtime系统的信息 <ul><li>这些方法允许对象执行内省操作 <ul><li><code>class</code>方法 <ul><li>返回一个对象的类</li></ul></li><li><code>isKindOfClass</code>和<code>isMemberOfClass</code>方法 <ul><li>检查对象是否在指定的类继承体系中</li></ul></li><li><code>respondsToSelector</code><ul><li>指示对象是否能够相应特定消息</li></ul></li><li><code>conformsToProtocol</code><ul><li>检查对象是否实现了指定协议类的方法</li></ul></li><li><code>methodForSelector</code><ul><li>返回指定方法实现的地址</li></ul></li></ul></li></ul></li></ul><h3 id="runtime函数" tabindex="-1"><a class="header-anchor" href="#runtime函数" aria-hidden="true">#</a> Runtime函数</h3><p>Runtime是一个具有公共接口的<strong>动态共享库</strong>，该接口由 位于目录<code>/usr/include/objc</code>中<strong>头文件</strong>中的 一组<strong>函数和数据结构</strong>组成。其中许多函数允许程序员使用普通的C语言来自己实现Objective-C代码编译时编译器的操作。而其他函数则构成了OC的功能基础。这些函数使得程序员能够开发其他接口到Runtime系统，成为增强开发环境的工具。在编写Objective-C代码时，程序员通常通常不需要直接使用这些基础函数。不过有时在编写Objective-C程序时，一些Runtime函数可能会有用。所有这些函数都在&quot;Objective-C Runtime Reference&quot;中有文档记录【苹果的这个链接已经挂掉了哈哈哈（一点都不好笑）】。</p><p>【这段怎么写都有点别扭】</p><h2 id="messaging消息函数" tabindex="-1"><a class="header-anchor" href="#messaging消息函数" aria-hidden="true">#</a> Messaging消息函数</h2><h3 id="引子-1" tabindex="-1"><a class="header-anchor" href="#引子-1" aria-hidden="true">#</a> 引子</h3><p>这一章描述了如何将消息表达式转换为<code>objc_msgSend</code>函数调用，如何通过方法名称引用方法。解释了如何利用<code>objc_msgSend</code>，如果有需要的话还可以实现规避动态绑定。</p><p><code>动态绑定，熟悉的词，想起C++的虚函数表，感觉这个机制比虚函数离谱多了</code></p><h3 id="the-objc-msgsend-function-消息发送函数" tabindex="-1"><a class="header-anchor" href="#the-objc-msgsend-function-消息发送函数" aria-hidden="true">#</a> The objc_msgSend Function 消息发送函数</h3><p>我们知道OC的底层是由C/C++实现的，而对于编译器来说方括号<code>[receiver message]</code>的实现则是以转化为一下形式</p><p><code>bjc_msgSend(receiver, selector)//不带参数</code></p><p><code>objc_msgSend(receiver, selector, arg1, arg2, ...)//带参数，应该是用可变参数列表实现的</code></p><p>消息函数执行<strong>动态绑定</strong>所需的操作如下：</p><ul><li>首先，它查找<code>selecor</code>指向的<code>过程procedure</code>（方法的实现）。由于不同类实现的同名函数的存在，具体找到哪个方法取决于<code>receiver</code>所在的类</li><li>然后，它调用该<code>过程</code>，将接收对象<code>(receiving object)</code>（指向其数据的指针）【是不是类似<code>C++的this指针</code>】和参数列表传递给该<code>过程</code>。</li><li>最后，它将过程的返回值传递作为自己的返回值</li></ul><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>Note: The compiler generates calls to the messaging function. You should never call it directly
in the code you write. —— 摘自苹果官方文档

这是提醒你永远不要直接在自己写的代码里尝试调用消息函数，不知道有没有好奇的程序员去试过。
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>消息的关键在于编译器为类和对象所构造的数据结构，该数据结构有两个基本元素（这又回到我前面写的OC基础了）</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>- 一个指向超类的指针
- 我们伟大而神秘的isa
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>isa </code>,也就是类分发表 <ul><li>【在我前面的文章中，提到了它指向的是元类】</li><li>【在苹果的文章中】，类分发表会讲方法选择弃和它们表示的方法的类的特定地址关联起来 <ul><li>例如，<code>setOrigin</code>方法的选择器与setOrigin<code>（实现）的地址相关联，</code>display<code>方法的选择器与</code>display\`的地址相关联</li></ul></li></ul></li></ul><p>当创建一个新对象时，会分配内存，初始化其实例变量。对象变量中的第一个是指向其类结构的指针。这个指针被称为<code>isa</code>，它使对象可以访问其类，通过类可以访问其继承的所有类。</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>Note: While not strictly a part of the language, the isa pointer is required for an object to 
work with the Objective-C runtime system. An object needs to be “equivalent” to a struct 
objc_object (defined in objc/objc.h) in whatever fields the structure defines. However, you 
rarely, if ever, need to create your own root object, and objects that inherit from NSObject or 
NSProxy automatically have the isa variable.

注意：虽然isa指针严格来说不属于语言的一部分，但它是对象与Runtime系统一起工作所必需的……
定义一个对象相当于struct objc_object（在objc/objc.h中定义）
从NSObject或NSProxy继承的对象会自动具有isa变量。
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><img src="https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/ObjCRuntimeGuide/Art/messaging1.gif" alt="messaging1" loading="lazy"> <img src="http://yulingtianxia.com/resources/Runtime/class-diagram.jpg" alt="class-diagram" style="zoom:50%;"></p><p>【看图其实还算好懂，前提是你把文章认真看了】</p><ul><li>当向对象发送消息时，消息函数会按照对象的<code>isa</code>指针到达类结构，然后在分发表中查找方法<code>selector</code><ul><li>如果在类中找不到<code>selector</code>，<code>objc_msgSend</code>会跟随指向超类的指针，并尝试在其分发表中查找选择器。连续的失败会导致<code>objc_msgSend</code>遍历类层次结构，直到达到NSObject类</li><li>一旦找到<code>selector</code>，函数将调用表中输入的方法，并将<code>接收对象的数据结构</code>传递给它</li></ul></li></ul><p>以上即是Runtime系统进行动态绑定的实现过程。<code>用术语来说就是：methods are dynamically bound to messages</code></p><p>为了加速消息传递过程，<code>Runtime</code>系统会缓存使用的<code>selectors</code>和方法的地址。每个类都有一个单独的<strong>缓存</strong>，它可以包含继承方法的<code>selector</code>以及类中定义的方法的<code>selector</code>。在搜索分发表之前，消息例程首<strong>先检查接收对象类的缓存</strong>（理论上，曾经使用过的方法可能会再次使用）。如果方法选择器在缓存中，消息传递只比函数调用略慢。一旦程序运行足够长时间以“热身”其缓存，几乎所有发送的消息都会找到缓存的方法。缓存会动态增长以适应程序运行时的新消息。</p><h3 id="使用隐藏参数" tabindex="-1"><a class="header-anchor" href="#使用隐藏参数" aria-hidden="true">#</a> 使用隐藏参数</h3><p>当<code>objc_msgSend</code>找到实现方法的过程时，它会调用该过程并将消息中的所有参数传递给它。它还会将两个隐藏参数传递给该过程：</p><ul><li>接收对象</li><li>方法的<code>selector</code></li></ul><p>这些参数使每个方法的实现都能明确了解调用它的消息表达式所拥有的两个部分。它们被称为<strong>隐藏参数</strong>，因为它们并不在定义方法的源代码中声明。它们是在编译代码时插入到实现中的 。</p><p>尽管这些参数没有明确声明，源代码仍然可以引用它们（就像它可以引用接收对象的实例变量一样）。一个方法将接收对象引用为<code>self</code>(<code>this指针这不就来了</code>)，将自己的选择器引用为<code>_cmd</code>。在下面的示例中，<code>_cmd</code>引用了<code>strange</code>方法的<code>selector</code>，<code>self</code>引用了接收<code>strange</code>消息的对象</p><div class="language-objective-c line-numbers-mode" data-ext="objective-c"><pre class="language-objective-c"><code>- (id)strange
{
    id  target = getTheReceiver();
    SEL method = getTheMethod();
 
    if ( target == self || method == _cmd )
        return nil;
    return [target performSelector:method];
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>self</code>是这两个参数中更有用的。实际上，它是将接收对象的实例变量提供给方法定义的方式。<code>这不还是this指针</code></p><h3 id="获取方法地址" tabindex="-1"><a class="header-anchor" href="#获取方法地址" aria-hidden="true">#</a> 获取方法地址</h3><p>规避动态绑定的唯一方法是获取方法的地址，然后直接调用它，就像它是一个函数一样。这可能适用于当特定方法将在连续执行多次并且程序员希望避免每次执行方法时的消息传递开销的罕见情况。</p><p>使用NSObject类中定义的方法</p><ul><li><code>methodForSelector:</code>，就可以请求指向实现方法的过程的指针，然后就能通过使用指针调用该过程。 <ul><li>通过该方法返回的指针必须小心地<strong>强制转换为正确的函数类型</strong>。强制转换应包括返回和参数类型。</li></ul></li></ul><p><code>从这里开始，这些方法我就保留了原文方法名中的冒号，而不是只保留方法名，在了解了消息机制后，冒号的存在还真无法忽视</code></p><p>下面的示例显示了如何直接调用实现<code>setFilled:</code>方法的过程：</p><div class="language-objective-c line-numbers-mode" data-ext="objective-c"><pre class="language-objective-c"><code>void (*setter)(id, SEL, BOOL);//声明了函数指针，参数分别是两个“隐藏参数”和一个方法原参数
int i;
 
setter = (void (*)(id, SEL, BOOL))[target
    methodForSelector:@selector(setFilled:)];
for (i = 0; i &lt; 1000; i++)
    setter(targetList[i], @selector(setFilled:), YES);

//请注意，methodForSelector:由Cocoa运行时系统提供，它不是Objective-C语言本身的特性
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>传递给过程的前两个参数是接收对象（self）和方法选择器（_cmd）。这些参数在方法语法中是隐藏的，但在调用方法作为函数时必须显式提供。</p><h2 id="dynamic-method-resolution-动态方法解析" tabindex="-1"><a class="header-anchor" href="#dynamic-method-resolution-动态方法解析" aria-hidden="true">#</a> Dynamic Method Resolution 动态方法解析</h2><p>本章描述了如何可以动态提供方法的实现。</p><h3 id="dynamic-method-resolution" tabindex="-1"><a class="header-anchor" href="#dynamic-method-resolution" aria-hidden="true">#</a> Dynamic Method Resolution</h3>`,60),p={href:"https://developer.apple.com/library/archive/documentation/Cocoa/Conceptual/ObjectiveC/Introduction/introObjectiveC.html#//apple_ref/doc/uid/TP30001163",target:"_blank",rel:"noopener noreferrer"},g=e("code",null,"@dynamic",-1),j=n(`<div class="language-objective-c line-numbers-mode" data-ext="objective-c"><pre class="language-objective-c"><code>@dynamic propertyName;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><strong>这告诉编译器与该属性相关的方法将会以动态方式提供</strong>。</p><p>程序员可以通过实现<code>resolveInstanceMethod:</code>和<code>resolveClassMethod:</code>方法来动态地为给定<code>selector</code>提供实例方法和类方法的实现。</p><p>Objective-C方法本质上是接受至少两个参数的C函数——<code>self</code>和<code>_cmd</code>。您可以使用函数<code>class_addMethod</code>将一个函数添加到一个类作为方法。因此，给定以下函数</p><div class="language-objective-c line-numbers-mode" data-ext="objective-c"><pre class="language-objective-c"><code>void dynamicMethodIMP(id self, SEL _cmd) {
    // 实现....
}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>然后就可以使用<code>resolveInstanceMethod:</code>将它动态添加到类作为一个名为<code>resolveThisMethodDynamically</code>的方法，像这样：</p><div class="language-objective-c line-numbers-mode" data-ext="objective-c"><pre class="language-objective-c"><code>@implementation MyClass
+ (BOOL)resolveInstanceMethod:(SEL)aSEL
{
    if (aSEL == @selector(resolveThisMethodDynamically)) {
          class_addMethod([self class], aSEL, (IMP) dynamicMethodIMP, &quot;v@:&quot;);
          return YES;
    }
    return [super resolveInstanceMethod:aSEL];
}
@end
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>方法转发（如在&quot;消息转发&quot;中描述）和动态方法解析在很大程度上是独立的。<strong>在转发机制生效之前</strong>，类有机会<strong>动态解析</strong>方法。如果调用了<code>respondsToSelector:</code>或<code>instancesRespondToSelector:</code>，则动态方法解析器有机会首先为选择器提供一个<code>IMP</code>。如果您实现了<code>resolveInstanceMethod:</code>，<strong>但希望特定选择器实际上通过转发机制进行转发，您可以为这些选择器返回NO</strong>。</p><h4 id="补充-imp是什么" tabindex="-1"><a class="header-anchor" href="#补充-imp是什么" aria-hidden="true">#</a> 补充：IMP是什么</h4><p>摘自杨老师的文章：</p><p><code>IMP</code>在<code>objc.h</code>中的定义是：</p><div class="language-objective-c line-numbers-mode" data-ext="objective-c"><pre class="language-objective-c"><code>typedef void (*IMP)(void /* id, SEL, ... */ );
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,12),f={href:"http://yulingtianxia.com/blog/2014/04/17/han-shu-zhi-zhen-yu-zhi-zhen-han-shu/",target:"_blank",rel:"noopener noreferrer"},_=e("code",null,"IMP",-1),S=n('<p>你会发现 <code>IMP</code> 指向的方法与 <code>objc_msgSend</code> 函数类型相同，参数都包含 <code>id</code> 和 <code>SEL</code> 类型。每个方法名都对应一个 <code>SEL</code> 类型的方法选择器，而每个实例对象中的 <code>SEL</code> 对应的方法实现肯定是唯一的，<strong>通过一组 <code>id</code> 和 <code>SEL</code> 参数就能确定唯一的方法实现地址；反之亦然</strong>。</p><h3 id="dynamic-loading-动态加载" tabindex="-1"><a class="header-anchor" href="#dynamic-loading-动态加载" aria-hidden="true">#</a> Dynamic Loading 动态加载</h3><p>注：这段基本照搬GPT翻译</p><p>Objective-C程序可以<strong>在运行时加载和链接新的类和分类</strong>。新的代码被合并到程序中，与在启动时加载的类和分类一样对待。</p><p>动态加载可以用于执行各种不同的任务。例如，System Preferences应用程序中的各个模块是动态加载的。</p><p>在Cocoa环境中，动态加载通常用于允许应用程序进行定制。<strong>其他人可以编写模块，您的程序在运行时加载这些模块</strong>，就像Interface Builder加载自定义调色板和OS X System Preferences应用程序加载自定义首选项模块一样。可加载模块扩展了您的应用程序的功能。它们以「您允许但自己可能没有预期或定义的方式」为其做出贡献。您提供了框架，但其他人提供了代码。【不得不说直译还是看起来别扭】</p><p>尽管存在一个执行Objective-C模块动态加载的runtime函数（<code>objc_loadModules</code>，在<code>objc/objc-load.h</code>中定义），Cocoa的<code>NSBundle</code>类提供了一个更为方便的动态加载接口，这是一种面向对象的接口，与相关服务集成在一起。有关<code>NSBundle</code>类及其用法的信息，请参阅Foundation框架参考中的<code>NSBundle</code>类规范。有关Mach-O文件的信息，请参阅OS X ABI Mach-O文件格式参考。</p><h2 id="message-forwarding-消息转发" tabindex="-1"><a class="header-anchor" href="#message-forwarding-消息转发" aria-hidden="true">#</a> Message Forwarding 消息转发</h2><p><code>Sending a message to an object that does not handle that message is an error. However, before announcing the error, the runtime system gives the receiving object a second chance to handle the message.</code>也就是说，如果我们向一个对象发送了错误的消息，消息转发机制能够作为容错了来第二次处理这个消息。</p><h3 id="forwarding-转发机制" tabindex="-1"><a class="header-anchor" href="#forwarding-转发机制" aria-hidden="true">#</a> Forwarding 转发机制</h3>',10);function O(y,C){const d=a("ExternalLinkIcon");return t(),s("div",null,[l,e("p",null,[e("a",u,[i("Objective-C Runtime"),o(d)])]),m,h,e("p",null,[e("a",v,[i("Objective-C Runtime Programming Guide"),o(d)])]),b,e("p",null,[i("有些情况下，您可能希望动态提供方法的实现。例如，Objective-C中的声明属性特性（参见"),e("a",p,[i("The Objective-C Programming Language"),o(d)]),i("中的“Declared Properties”）包括"),g,i("指令")]),j,e("p",null,[i("它就是一个"),e("a",f,[i("函数指针"),o(d)]),i("，这是由编译器生成的。当你发起一个 OC 消息之后，最终它会执行的那段代码，就是由这个函数指针指定的。而 "),_,i(" 这个函数指针就指向了这个方法的实现。既然得到了执行某个实例某个方法的入口，我们就可以绕开消息传递阶段，直接执行方法，也就是上面提到的动态方法解析。")]),S])}const R=c(r,[["render",O],["__file","Runtime.html.vue"]]);export{R as default};
